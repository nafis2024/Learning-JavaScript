class calculator{
    constructor(input, operator, input2, result){
        this.input = firstInput
        this.operator = firstSign
        this.input2 = secondInput
        this.result = answer
    }
}















const prompt = require("prompt-sync")();

const firstInput = prompt('Give out a number to work with..') 
const firstSign = prompt("The operation you want to do??")
const secondInput = prompt('Give out another number..')
const answer = functionDeployAns





var functionDeployAns //This is supposed to be moved to another place later in the project