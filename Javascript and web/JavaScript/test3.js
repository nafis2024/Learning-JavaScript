const prompt = require("prompt-sync")();

var d = "Dhaka"
var c = "chattogram"
var n = "natore"
var b = "ban..."
var co = "coxsbazar"
var a ="huh"

var questions = prompt("??");
var works;
var anythingg = anything != cities;
var cities = [d, c, n, b, co, a]
for (i=0; i <= 3; i++) {
    if (questions == cities[i]){
        works = "yes!!"
        console.log(works, "got em")
    }
    if (questions !== cities[i]) {
        works = "mhmm!!"
        console.log(works, "another one")
        break;
    }
}
